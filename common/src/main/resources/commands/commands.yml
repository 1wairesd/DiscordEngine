commands:
  hello:
    description: "Приветствует пользователя"
    type: "both" # slash | prefix | both
    context: "server" # server | dm | both
    actions:
      - send_message:
          content: "👋 Привет, {user}!"