config:
  version: 1
  type: discord-flow

flow:
  id: register

  steps:

    start:
      action:
        open_modal: user_register

    process_registration:
      actions:
        - send_message:
            content: "✅ Registration completed! Welcome to the server, {username}!"
        - send_message:
            content: "📝 Your info: Age: {age}, Bio: {bio}"
        - add_role:
            role_id: "123456789" # Replace with actual role ID